server:
  port: 8010
spring:
  application:
    name: order
  datasource:
    name: ordersystem
    url: jdbc:mysql://localhost:3306/ordersystem?userUnicode=true&characterEncoding=UTF-8
    username: root
    password: 902477
    driver-class-name: com.mysql.jdbc.Driver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
mybatis:
  mapper-locations: classpath:/mapping/*.xml
  type-aliases-package: com.zhdsmart.entity